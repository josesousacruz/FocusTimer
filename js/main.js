import * as FocusTimer from './FocusTimer/index.js'
// O index do FocusTimer contem a função que seta o cronometro passando os minutos.
import "./toggleMode.js";
//Importa o toggleMode que é responsavel pela mudança de modo dark e light


FocusTimer.start(25,0)
//Seta o cronometro com valor padrão



